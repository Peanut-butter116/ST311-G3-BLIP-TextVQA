# ST311-G3-BLIP-TextVQA

Please run the demo.py file only which contains a simple version of fine-tuned BLIP model for TextVQA task. It turns out the acuraccy is smaller then the based model.

The fine_tuning.py file is a more complex version of the fine-tuned BLIP model for TextVQA task, whih I have not tested its performance yet given that the computational cost of GPU is around 20 hours.